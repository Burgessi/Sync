<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.pro.sync.alarm.dao.AlarmDaoImpl">

<select id="getUnreadAlarms" resultType="AlarmVo">
        SELECT a.ALARM_ID, a.RECEIVER_ID, a.ALARM_TYPE, a.TITLE, a.CONTENT, a.STATUS, a.CREATE_AT
        FROM alarms a
        JOIN employees e ON a.RECEIVER_ID = e.EMP_ID
        WHERE a.RECEIVER_ID = #{emp_id}
        AND a.STATUS = 0
    </select>

    <select id="getUnreadAlarmsCnt" resultType="Integer">
        SELECT COUNT(*) 
        FROM ALARM
        WHERE EMP_ID = #{emp_id}
        AND STATUS = 'N'
    </select>

    <update id="updateStatus">
        UPDATE ALARM
        SET STATUS = 'Y'
        WHERE ALARM_ID = #{alarm_id}
    </update>
 
</mapper>
